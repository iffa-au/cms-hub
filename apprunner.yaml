version: 1.0
runtime: nodejs22
build:
  commands:
    pre-build:
      - cd backend && npm ci --include=dev
    build:
      - echo "Using ts-node at runtime; no compile step needed"
run:
  command: cd backend && npm start
  network:
    port: 8000